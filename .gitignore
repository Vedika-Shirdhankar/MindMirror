# Dependencies
node_modules/

# Build output
dist/

# Environment variables — NEVER commit real secrets
.env
.env.*
!.env.example

# Uploaded videos (local dev only — ephemeral on Render)
backend/backend/uploads/

# OS / editor artifacts
*.local
.DS_Store
Thumbs.db

# ML model weights (too large for Git — keep locally or use Git LFS)
ml/mindmirror_distilbert_model/model.safetensors
ml/mindmirror_distilbert_model/training_args.bin

# ML dataset (large CSV)
ml/Mental_Health_Condition_Classification.csv

# Python cache
__pycache__/
*.pyc